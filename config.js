/* =====================================================================
 * 集計サーバーの設定
 *
 * endpoint が空文字のあいだは、結果画面にデータ提供の質問そのものが出ない。
 * 送信も統計取得も一切行わないので、このままでも単体で動く。
 *
 * 集計を有効にするには server/apps-script.gs を Google Apps Script として
 * デプロイし、発行された /exec の URL をここに貼る。手順は README を参照。
 * ===================================================================== */

const COLLECT = {
  /* 例: 'https://script.google.com/macros/s/XXXXXXXX/exec' */
  endpoint: '',

  /* 参加者の平均を表示し始める最小人数。サーバー側でも同じ下限を掛ける。
     少人数のうちは、平均から個人の回答が逆算できてしまうため。 */
  minN: 5
};
